# ~/.bash_profile (managed by stow)
# Stub: source real config under ~/.config/bash

[ -r "$HOME/.config/bash/.bash_profile" ] && . "$HOME/.config/bash/.bash_profile"

# Also source bashrc for login shells (common practice)
[ -r "$HOME/.bashrc" ] && . "$HOME/.bashrc"