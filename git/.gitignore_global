# Global gitignore — XDG auto-discovered at $XDG_CONFIG_HOME/git/ignore

# macOS
.DS_Store

# Confluence export directories (generated by confluence MCP tool)
__confluence_content/

# VS Code workspace files — local only, never push
*.code-workspace
.vscode/settings.json

# Claude Code — local session settings (machine-specific)
**/.claude/settings.local.json

# direnv — local environment overrides
.envrc

# gitlab-ci-local artifacts
.gitlab-ci-local-variables.yml
.gitlab-ci-local/

# Pyright config (local IDE tooling)
pyrightconfig.json
