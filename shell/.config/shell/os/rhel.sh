# shellcheck shell=bash
# RHEL/CentOS specific overlay (builds on linux.sh)
# RHEL enterprise environment settings
export TMOUT="${TMOUT:-0}" # Disable shell timeout unless admin policy already set it
